div.input(class=input.class )
    label.input-title #{input.title}
    if input.icon
        - icon = {type: input.icon, size: "14px"}
        include ../../components/icon/icon.pug
    input(type=input.type name=input.name placeholder=input.title)
